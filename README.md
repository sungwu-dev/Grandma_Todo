# Grandma Todo (Next.js)

## 실행 방법
1. `npm install`
2. `npm run dev`
3. 브라우저에서 `http://localhost:3000` 접속

## 참고
- 일정 편집은 `/family`에서 저장하며 localStorage(`schedule_v1`)에 보관됩니다.
- 완료 체크는 날짜별로 `done_YYYY-MM-DD` 키에 저장됩니다.